# 项目状态(当前设计的唯一真相)

> 由 tidykeep 协议维护。任何 Agent 开工前必读本文件。
> 本文件只描述"**现在**"——历史在 git 与下方决策记录里,废弃方案在墓地里。
> 首次安装后,请让 Agent 执行"tidykeep 初始化扫描"自动填充本文件。

## 当前架构与设计

(初始化扫描后填写:项目做什么、模块划分、数据流、关键入口、重要约定。
只写"当前有效"的内容;任何段落一旦过期,必须更新或移入墓地。)

## 设计决策记录(ADR-lite)

状态只允许两种:`active`、`superseded → D-xxx`。方案变更时**新增一行**并把旧行标记为 superseded,不要删除历史行。

| 编号 | 日期 | 决策 | 状态 |
|---|---|---|---|
| D-001 | YYYY-MM-DD | (示例)训练参数全部通过 CLI 传入,禁止复制脚本调参 | active |

## 墓地(已废弃/已删除,禁止复活)

Agent 不得参考、不得重新实现下表中的内容。

| 日期 | 删除/废弃了什么 | 原因 | 取代者 |
|---|---|---|---|
| YYYY-MM-DD | (示例)train_v2.py、train_final.py | 与 train.py 仅参数不同的历史副本 | train.py + CLI 参数(D-001) |
