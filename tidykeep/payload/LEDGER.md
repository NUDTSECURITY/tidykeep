# 文件台账（LEDGER）

> 由 tidykeep 协议维护。每个受管的代码文件与文档文件各占一节。
> **每次任务收尾必须更新**：完成项从 TODO 移入 DONE（附日期），新待办登记进 TODO，刷新"最后核对"。
> 条目格式请勿更改（hooks 与审计流程依赖它）。删除文件时，整节移除并在 STATE.md 墓地登记。

## 使用规则

- 一个文件一节，标题格式：`## <相对路径>`。
- `最后核对` 表示"该条目最近一次与文件实际状态对齐"的日期——审计时用它找出长期无人核对的文件。
- DONE 只增不删（它就是这个文件的变更简史）；条目过多时可归档到节尾的折叠段落。

---

## 示例: src/example.py

- 最后核对: 2026-08-12
- TODO:
  - [ ] （示例）支持断点续训
- DONE:
  - [x] 2026-08-12 （示例）参数改为 CLI 传入，合并并删除了 train_v2.py

（首次安装后，让 Agent 执行"tidykeep 初始化扫描"自动为现有文件生成条目，然后删除本示例节。）
